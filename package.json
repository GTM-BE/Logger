{
  "devDependencies": {
    "@types/node": "15.0.2",
    "husky": "^6.0.0",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "format": "prettier --config ./.prettierrc --write ./src/*",
    "dev": "",
    "build": "tsc --build src"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format"
    }
  }
}
